<?xml version="1.0" encoding="UTF-8"?>
<!--
  Root Maven build file for the DigiBank workshop application.

  This parent POM defines the shared Java version, dependency versions, module order, and compiler
  settings used by every module. Security-sensitive dependencies are pinned here so vulnerability scan
  fixes are applied consistently across the whole repository instead of being scattered in child POMs.

  The project is a modular monolith: common shared code builds first, then customer, account, transfer,
  and finally the web assembly module that packages the runnable Spring Boot application.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.m2ibank</groupId>
    <artifactId>digibank-parent</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>digibank-parent</name>
    <description>Parent POM for DigiBank Modular Monolith Application</description>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.14</version>
        <relativePath/>
    </parent>

    <properties>
        <java.version>17</java.version>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <jackson-bom.version>2.21.4</jackson-bom.version>
        <micrometer.version>1.15.12</micrometer.version>
        <springdoc-openapi.version>2.8.16</springdoc-openapi.version>
        <swagger-annotations.version>2.2.43</swagger-annotations.version>
        <postgresql.version>42.7.12</postgresql.version>
        <spring-data-bom.version>2025.0.12</spring-data-bom.version>
        <spring-framework.version>6.2.19</spring-framework.version>
        <tomcat.version>10.1.59</tomcat.version>
        <cucumber.version>7.15.0</cucumber.version>
        <lombok.version>1.18.46</lombok.version>
        <spotbugs-maven-plugin.version>4.9.3.2</spotbugs-maven-plugin.version>
        <maven-pmd-plugin.version>3.28.0</maven-pmd-plugin.version>
        <findsecbugs-plugin.version>1.14.0</findsecbugs-plugin.version>
        <dependency-check.version>10.0.4</dependency-check.version>
        <maven-surefire-plugin.version>3.5.5</maven-surefire-plugin.version>
    </properties>

    <modules>
        <module>common-module</module>
        <module>customer-module</module>
        <module>account-module</module>
        <module>transfer-module</module>
        <module>digibank-web</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <!-- Internal Modules -->
            <dependency>
                <groupId>com.m2ibank</groupId>
                <artifactId>common-module</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.m2ibank</groupId>
                <artifactId>customer-module</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.m2ibank</groupId>
                <artifactId>account-module</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.m2ibank</groupId>
                <artifactId>transfer-module</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- OpenAPI / Swagger -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc-openapi.version}</version>
            </dependency>
            <dependency>
                <groupId>io.swagger.core.v3</groupId>
                <artifactId>swagger-annotations-jakarta</artifactId>
                <version>${swagger-annotations.version}</version>
            </dependency>

            <!-- Cucumber Integration Testing -->
            <dependency>
                <groupId>io.cucumber</groupId>
                <artifactId>cucumber-java</artifactId>
                <version>${cucumber.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>io.cucumber</groupId>
                <artifactId>cucumber-junit-platform-engine</artifactId>
                <version>${cucumber.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>io.cucumber</groupId>
                <artifactId>cucumber-spring</artifactId>
                <version>${cucumber.version}</version>
                <scope>test</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <!-- Explicit aggregate gate, shared by local runs and the PMD CI job. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-pmd-plugin</artifactId>
                <version>${maven-pmd-plugin.version}</version>
                <configuration>
                    <targetJdk>${java.version}</targetJdk>
                    <rulesets>
                        <ruleset>${maven.multiModuleProjectDirectory}/pmd-ruleset.xml</ruleset>
                    </rulesets>
                    <includeTests>false</includeTests>
                    <failOnViolation>true</failOnViolation>
                    <failurePriority>3</failurePriority>
                    <maxAllowedViolations>0</maxAllowedViolations>
                    <printFailingErrors>true</printFailingErrors>
                    <failOnError>true</failOnError>
                    <linkXRef>false</linkXRef>
                </configuration>
            </plugin>
            <!-- Static analysis: Java bug patterns plus Find Security Bugs security detectors.
                 Not bound to a lifecycle phase; run explicitly via `mvn spotbugs:check` (see
                 docs/security/devsecops.md) so it stays a separate, opt-in CI gate. -->
            <plugin>
                <groupId>com.github.spotbugs</groupId>
                <artifactId>spotbugs-maven-plugin</artifactId>
                <version>${spotbugs-maven-plugin.version}</version>
                <configuration>
                    <effort>Max</effort>
                    <threshold>Medium</threshold>
                    <xmlOutput>true</xmlOutput>
                    <excludeFilterFile>${maven.multiModuleProjectDirectory}/spotbugs-exclude.xml</excludeFilterFile>
                    <plugins>
                        <plugin>
                            <groupId>com.h3xstream.findsecbugs</groupId>
                            <artifactId>findsecbugs-plugin</artifactId>
                            <version>${findsecbugs-plugin.version}</version>
                        </plugin>
                    </plugins>
                </configuration>
            </plugin>
            <!-- OWASP Dependency-Check: identifies known vulnerabilities in direct and transitive
                 Maven dependencies. Like SpotBugs and PMD, it is NOT bound to the default verify
                 lifecycle so a normal `mvn clean verify` stays fast and does not require network
                 access to the NVD. It runs explicitly via `mvn org.owasp:dependency-check-maven:check`
                 (locally and in the dedicated CI job) and fails the build on CVSS >= 7. -->
            <plugin>
                <groupId>org.owasp</groupId>
                <artifactId>dependency-check-maven</artifactId>
                <version>${dependency-check.version}</version>
                <configuration>
                    <format>HTML</format>
                    <failBuildOnCVSS>7</failBuildOnCVSS>
                    <skipProvidedScope>true</skipProvidedScope>
                    <skipTestScope>true</skipTestScope>
                    <nvdApiKey>${env.NVD_API_KEY}</nvdApiKey>
                    <ossindexAnalyzerEnabled>false</ossindexAnalyzerEnabled>
                    <suppressionFiles>
                        <suppressionFile>${maven.multiModuleProjectDirectory}/dependency-check-suppressions.xml</suppressionFile>
                    </suppressionFiles>
                </configuration>
            </plugin>
            <!-- Surefire: stabilize test execution between local runs and GitHub Actions. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven-surefire-plugin.version}</version>
                <configuration>
                    <useModulePath>false</useModulePath>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
