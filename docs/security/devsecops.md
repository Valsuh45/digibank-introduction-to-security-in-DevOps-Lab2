# DevSecOps and Security Notes

## Input Validation

DTOs use Jakarta Bean Validation to reject missing, malformed, or unsafe input before it reaches business logic.

Examples:

- Customer email must be valid and short enough for the database column.
- Account opening balance must not be negative.
- Transfer amount must be positive.
- Transfer description is length-limited.

## Business Rule Protection

Service classes enforce rules that need domain knowledge:

- Duplicate customers are rejected.
- Account numbers are generated by the server.
- Negative balances are rejected.
- Transfers between the same account are rejected.
- Insufficient-balance transfers are rejected.

## Database Protection

Flyway migrations create constraints that back up Java validation:

- Unique customer emails and identity numbers.
- Unique account numbers.
- Non-negative account balances.
- Positive transfer amounts.
- Different source and target accounts.
- Foreign keys between customers, accounts, and transfers.

## Safe Error Handling

`GlobalExceptionHandler` returns controlled API responses. Known business errors get clear messages. Unexpected failures are logged internally and returned as a generic client message.

Spring Boot server error settings also disable public exception names, messages, and stack traces.

## CI Security Gates

GitHub Actions runs:

- Maven verification.
- Trivy filesystem scan for vulnerabilities, secrets, and misconfigurations.
- Trivy container image scan.
- Docker Compose smoke test against health, OpenAPI, and transfer workflow endpoints.

These checks make the security evidence reproducible outside a local developer machine.
