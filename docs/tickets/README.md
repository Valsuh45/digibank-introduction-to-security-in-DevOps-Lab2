# Active Tickets

The original five implementation tickets are complete and archived in `docs/archive/completed-tickets/`.

Only follow-up work that is still useful should remain open here.

## Current Follow-Up Tickets

1. `#12` Publish the Docker image to GitHub Container Registry.
2. `#13` Add a deployment runbook for using the GHCR image.
3. `#14` Add signed release evidence or SBOM artifacts if the workshop requires stronger supply-chain proof.

## Ticket Rule

Do not create tickets for work that is already implemented. Completed work belongs in the archive or evidence docs, not in the active ticket list.
